(					; -*-scheme-*-
 ;; ("*-ct"  long                ) ; 42 uses
 ;; ("*-id"  long                ) ; 7 uses
 ;; ("caller" (PTR char)	      )
 ;; ("*ame"  (PTR "unsigned char")         ) ; 8 uses
 ;; ("*ara"  ARRAY               ) ; 4 uses
 ;; ("*blk"  (ARRAY "unsigned char")       ) ; 62 uses
 ;; ("*buk"  (PTR "ENTRY")       ) ; 13 uses
 ;; ("*ent"  (PTR "ENTRY")       ) ; 127 uses
 ;; ("*fct"  long                ) ; 24 uses
 ;; ("*fun"  (FUNCTION int)      ) ; 1 uses
 ;; ("*han"  (PTR "HAND")        ) ; 24 uses
 ;; ("*ind"  (PTR "ENTRY")       ) ; 1 uses
 ;; ("*lck"  (PTR "LCK")         ) ; 16 uses
 ;; ("*nts"  (PTR "ENTRY")       ) ; 5 uses
 ;; ("*num"  long                ) ; 31 uses
 ;; ("*-double"  double          ) ;
 ;; ("*-float"  float            ) ;
 ;; ("*ong"  long                ) ; 7 uses
 ;; ("*ort"  short               ) ; 1 uses
 ;; ("*pkt"  (ARRAY int)         ) ; 17 uses
 ;; ("*son"  (PTR char)	     )
 ;; ("*str"  (ARRAY "unsigned char")       ) ; 64 uses
 ;; ("key"  (ARRAY "unsigned char")       ) ; 64 uses
 ;; ("*tab"  PTR                 ) ; 15 uses
 ;; ("*unc"  (FUNCTION int)      ) ; 7 uses
 ;; ("seg-chain"  (PTR "SEGD"))
 ;; ("*seg"  (PTR "SEGD"))
 ;; ("*segd"  (PTR "SEGD"))
 ;; ("close-seg*" int)
 ;; ("reason" (PTR char))
 ;; ("*matrix" (ARRAY (ARRAY double)))
 ;; ;(main int)

 ;(*int int)

 ;; ;("list" (FUNCTION "boost::fusion::make_list"))
 ;; ;("list" "make_list")

 )
